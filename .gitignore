node_modules/

# Локальный тестовый корпус: экспорты чатов в репозиторий не попадают.
# `tools/bench/chats/` — временная копия на время замера, `*.jsonl` — сами экспорты.
tools/bench/chats/
*.jsonl

# Пути к корпусу и имена карточек конкретной машины. Формат — в `tools/corpus.mjs`.
tools/corpus.local.mjs

# Локальные рабочие заметки — не документация, в репозиторий не попадают.
/anti-slop.md
/plan-anti-slop.md
/etap*.md
